// Source code is decompiled from a .class file using FernFlower decompiler (from Intellij IDEA).
public class DeterminantSolver {
   public DeterminantSolver() {
   }

   public static void main(String[] var0) {
      int[][] var1 = new int[][]{{3, 1, 2}, {2, 4, 1}, {5, 2, 3}};
      System.out.println("===================================================");
      System.out.println("  3x3 MATRIX DETERMINANT SOLVER");
      System.out.println("  Student: John Rowelle A. Ambuan");
      System.out.println("  Assigned Matrix:");
      System.out.println("===================================================");
      printMatrix(var1);
      System.out.println("===================================================");
      System.out.println();
      System.out.println("Expanding along Row 1 (cofactor expansion):");
      System.out.println();
      int var2 = solveDeterminant(var1);
      System.out.println();
      System.out.println("===================================================");
      System.out.println("  DETERMINANT = " + var2);
      System.out.println("===================================================");
      if (var2 == 0) {
         System.out.println("  The matrix is SINGULAR — it has no inverse.");
      }

   }

   public static void printMatrix(int[][] var0) {
      System.out.println("  | " + var0[0][0] + "  " + var0[0][1] + "  " + var0[0][2] + " |");
      System.out.println("  | " + var0[1][0] + "  " + var0[1][1] + "  " + var0[1][2] + " |");
      System.out.println("  | " + var0[2][0] + "  " + var0[2][1] + "  " + var0[2][2] + " |");
   }

   public static int solveDeterminant(int[][] var0) {
      int var1 = 0;

      for(int var2 = 0; var2 < 3; ++var2) {
         int var3 = computeMinor(var0, 0, var2);
         int var4 = (int)Math.pow((double)-1.0F, (double)var2) * var0[0][var2] * var3;
         System.out.println("  Step " + (var2 + 1) + " - Minor M" + (var2 + 1) + ": det([" + var0[1][(var2 + 1) % 3] + "," + var0[1][(var2 + 2) % 3] + "],[" + var0[2][(var2 + 1) % 3] + "," + var0[2][(var2 + 2) % 3] + "]) = (" + var0[1][(var2 + 1) % 3] + "*" + var0[2][(var2 + 2) % 3] + ") - (" + var0[1][(var2 + 2) % 3] + "*" + var0[2][(var2 + 1) % 3] + ") = " + var0[1][(var2 + 1) % 3] * var0[2][(var2 + 2) % 3] + " - " + var0[1][(var2 + 2) % 3] * var0[2][(var2 + 1) % 3] + " = " + var3);
         System.out.println("  Cofactor C" + (var2 + 1) + " = (" + (var2 % 2 == 0 ? "+" : "-") + "1) * " + var0[0][var2] + " * " + var3 + " = " + (var2 % 2 == 0 ? "" : "-") + var0[0][var2] * var3);
         var1 += var4;
      }

      System.out.println();
      System.out.println("  det(M) = " + var1);
      return var1;
   }

   public static int computeMinor(int[][] var0, int var1, int var2) {
      int[][] var3 = new int[2][2];
      int var4 = 0;
      int var5 = 0;

      for(int var6 = 0; var6 < 3; ++var6) {
         if (var6 != var1) {
            var5 = 0;

            for(int var7 = 0; var7 < 3; ++var7) {
               if (var7 != var2) {
                  var3[var4][var5] = var0[var6][var7];
                  ++var5;
               }
            }

            ++var4;
         }
      }

      return var3[0][0] * var3[1][1] - var3[0][1] * var3[1][0];
   }
}
